import XCTest
@testable import SceneVideoEngine

final class SceneVideoEngineTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SceneVideoEngine().text, "Hello, World!")
    }
}
